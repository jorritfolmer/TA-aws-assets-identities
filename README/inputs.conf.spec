[ec2://<name>]
role_to_list_accounts = Role to assume in the AWS master account which allows list-accounts. E.g. arn:aws:iam::123456789:role/tf-list-accounts-role
role_to_describe_ec2 = The role to assume in each account. E.g. tf-ec2-describe-role

[accounts://<name>]
role_to_list_accounts = Role to assume in the AWS master account which allows list-accounts. E.g. arn:aws:iam::123456789:role/tf-list-accounts-role